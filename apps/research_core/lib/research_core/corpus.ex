defmodule ResearchCore.Corpus do
  @moduledoc """
  Pure corpus-quality contracts and deterministic QA pipeline.

  This namespace turns raw retrieval outputs into inspectable canonical records,
  duplicate groups, quarantine records, and downstream-ready corpus buckets.
  It deliberately stops at corpus shaping and does not synthesize evidence.
  """
end
